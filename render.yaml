services:
  - type: web
    name: backend-preprod
    env: node
    rootDir: backend
    plan: free
    region: frankfurt
    buildCommand: |
      npm install
      npx prisma migrate deploy
    startCommand: npm run start
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: SESSION_SECRET
        sync: false
      - key: CORS_ORIGIN
        value: https://sacsabonheur.vercel.app
      - key: NODE_ENV
        value: production
